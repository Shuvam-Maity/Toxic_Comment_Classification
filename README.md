# Toxic_Comment_Classification
Binary toxic comment classifier trained on the Jigsaw Toxic Comment Classification dataset. Compares a classical TF-IDF + Logistic Regression baseline (with class-weighting and undersampling variants) against a fine-tuned RoBERTa transformer, benchmarking precision, recall, and F1.
